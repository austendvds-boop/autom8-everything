# B7 — Products Bento + SocialProofBar + HomePageClient Cleanup

**Thinking level:** medium
**Branch:** `ui/full-redesign`
**Gate:** `Set-Location 'C:\Users\austen\.openclaw\workspace-coder\projects\autom8-everything'; npm run build`

---

## Overview

Rewrite ServicesBento and SocialProofBar with warm amber colors. Delete obsolete components. Update HomePageClient to remove deleted component imports and fix section order. This batch handles the upper half of the page sections (below hero).

---

## Files to Modify

1. `src/components/ServicesBento.tsx` — color overhaul
2. `src/components/SocialProofBar.tsx` — rewrite
3. `src/app/HomePageClient.tsx` — remove deleted imports, update section order
4. **Delete:** `src/components/ProofBar.tsx`, `src/components/OfferLadder.tsx`, `src/components/Stats.tsx`, `src/components/PricingOverview.tsx`, `src/components/CadenceHighlight.tsx`

---

## 1. `src/components/ServicesBento.tsx` — Color Overhaul

### Global Find-Replace in this file

| Old Value | New Value |
|-----------|-----------|
| `#8B5CF6` | `#D4A030` |
| `#A78BFA` | `#E8C068` |
| `#06B6D4` | `#D4A030` (or remove if secondary accent) |
| `#C4B5FD` | `#E8C068` |
| `#D4D4D8` | `#9B978F` |
| `#A1A1AA` | `#9B978F` |
| `#111118` | `#161920` |
| `#0F0F18` | `#0E1015` |
| `#DDD6FE` | `#E8C068` |
| `#7C3AED` | `#B8892A` |
| `#71717A` | `#5E5B56` |
| `rgba(139, 92, 246, ...)` | `rgba(212, 160, 48, ...)` — match opacity values |
| `rgba(6, 182, 212, ...)` | `rgba(212, 160, 48, ...)` — at lower opacity |
| `rgba(167, 139, 250, ...)` | `rgba(232, 192, 104, ...)` |

### Section heading
Change `fontFamily: "var(--font-playfair), serif"` → `fontFamily: "var(--font-manrope), sans-serif"` on all h2/h3 elements.

### Section H2 text
Keep the existing copy: "Products That Keep Your Business Running Smoothly" — but change the gradient text words. The `<span className="gradient-text">Running Smoothly</span>` remains with gradient-text class (which now uses amber gradient from B4).

### Hero card (Cadence featured)
- Border gradient: change from purple/cyan to amber: `linear-gradient(135deg, rgba(212, 160, 48, 0.72), rgba(232, 192, 104, 0.42))`
- Background gradient inner: `linear-gradient(140deg, rgba(22, 20, 14, 0.96), rgba(14, 16, 21, 0.96))`
- Glow: `radial-gradient(circle_at_top_left, rgba(212, 160, 48, 0.20), transparent 60%)`
- Inner background: `bg-gradient-to-br from-[#D4A030]/[0.08] via-transparent to-[#E8C068]/[0.04]`
- Box shadows: replace all purple rgba with amber rgba at same opacities

### Small cards
- Hover border: `hover:border-[#D4A030]/30`
- Hover shadow: `hover:shadow-[0_0_40px_rgba(212,160,48,0.20)]`
- Hover gradient overlay: `from-[#D4A030]/[0.04]`
- Icon container: `bg-[#D4A030]/[0.12]`, icon color: `text-[#D4A030]`

### CTA buttons
- Primary: `bg-[linear-gradient(135deg,#D4A030,#E8C068)] text-[#0E1015]` (dark text on amber)
- Secondary: `border-white/20 text-[#EDEBE8] hover:border-[#D4A030]/50`

### Product data — "Web + Monthly SEO" rename
In the products array, the entry with `productName: "Web + Monthly SEO"` — keep as-is. The blueprint says "Website Creation" and "SEO & Content" as separate products, but the current code already has them merged. **Do not split them** — keep the existing product data structure. Only change colors.

### Check icons
Change check circle bg from `bg-[#8B5CF6]/20` → `bg-[#D4A030]/15`, text from `text-[#C4B5FD]` → `text-[#E8C068]`

---

## 2. `src/components/SocialProofBar.tsx` — Rewrite

Read the current file first. Apply these color changes:

| Old | New |
|-----|-----|
| Any `#8B5CF6` | `#D4A030` |
| Any `#06B6D4` | `#D4A030` |
| `#A1A1AA` | `#9B978F` |
| `#111118` | `#161920` |
| `#0A0A0F` | `#0E1015` |
| Purple rgba values | Amber rgba at same opacity |
| `fontFamily: "var(--font-playfair)"` | `fontFamily: "var(--font-manrope), sans-serif"` |

Star colors: if any star icons exist, use `text-[#F59E0B]` (the --star token).

---

## 3. Delete Obsolete Components

Delete these files entirely:
- `src/components/ProofBar.tsx`
- `src/components/OfferLadder.tsx`
- `src/components/Stats.tsx`
- `src/components/PricingOverview.tsx`
- `src/components/CadenceHighlight.tsx`

---

## 4. `src/app/HomePageClient.tsx` — Update

### Remove deleted imports
Remove these imports and their JSX usage:
- `ProofBar`
- `OfferLadder`
- `Stats` (if imported)
- `PricingOverview` (if imported)
- `CadenceHighlight` (if imported)

### Update section order
The page sections (top to bottom) should be:
```tsx
<Navigation />
<Hero />
<ScrollFade />
<SocialProofBar />
<ServicesBento />
<ScrollFade />
<HowItWorks />
<WhoItsFor />
<ScrollFade />
<Testimonials />
<FAQ />
<CTA />
<Footer />
<StickyMobileCTA />
```

Note: No `ProofBar` or `OfferLadder` in the list.

### Background color
Change `bg-[#0A0A0F]` → `bg-[#0E1015]`

### Skip link
Change `focus:bg-[#8B5CF6]` → `focus:bg-[#D4A030] focus:text-[#0E1015]`

---

## Gate

Run: `Set-Location 'C:\Users\austen\.openclaw\workspace-coder\projects\autom8-everything'; npm run build`

Build must pass with no import errors (deleted components must not be referenced anywhere).

---

## Git

Commit message: `feat: products bento amber overhaul, delete obsolete components, clean up HomePageClient`

Push to `ui/full-redesign`.

---

## Procurement Contract

Every coder batch must terminate immediately after the required gates for that batch are satisfied and the code is committed and pushed. Coders must not start local servers, run manual verification loops, open-ended exploratory tests, or invent post-gate validation steps.

Every batch must end with a git commit and push to the project branch. If a batch makes no code changes, the coder must write a status/log entry (e.g., append to docs/deploy-log.md) and commit+push it.

Coders MUST notify Steve as the final step after git push by POSTing to `http://localhost:18789/hooks/agent` with `Authorization: Bearer KyCiiWTXATTqCayTrPyoE9krcBzXFP7YPtzqEGlA1aA=` and body `{"message":"Batch complete. Gate ready.","agentId":"main","deliver":false}`. Write the JSON to a temp file and use `curl.exe --data-binary @file` to avoid shell escaping issues.

Coders do NOT need to set up git credentials. The orchestrator handles PAT configuration pre-flight.
