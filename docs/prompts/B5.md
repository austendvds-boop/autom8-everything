# B5: Pricing Page + PricingOverview Component

**Thinking level: medium**
**Gate:** `npm run build`
**Branch:** `ui/autom8-redesign`

## Critical Constraint
**Do NOT change any visual styling** — no color changes, no font changes, no layout changes, no animation changes, no spacing changes. Only change text content, data arrays, links, and CTAs. Keep all existing Tailwind classes, component structure, and motion presets exactly as-is.

## Files to Edit
1. `src/app/pricing/page.tsx`
2. `src/components/PricingOverview.tsx`

---

## 1. `src/app/pricing/page.tsx`

### Replace `websiteTiers` array entirely:

The old array has Launch $1,500 / Scale $3,500 / Custom. Replace with the contract tiers:

```ts
const websiteTiers = [
  {
    name: "Launch",
    price: "$500",
    details: ["Up to 5 pages", "Mobile-ready", "Contact/booking form", "Basic on-page SEO"],
  },
  {
    name: "Enterprise",
    price: "$1,000",
    details: ["Up to 10 pages", "Everything in Launch", "Custom features", "Built for your workflow"],
    featured: true,
  },
];
```

Note: This drops from 3 tiers to 2. The grid that renders `websiteTiers.map(...)` currently uses `md:grid-cols-3`. Change it to `md:grid-cols-2` so the two cards fill the space evenly.

### Update the Website Creation card:
- Change the h2 from `"Launch / Scale / Custom"` → `"Launch / Enterprise"`
- Change the CTA text from `"View Website Details"` → `"Get a Free Quote"` and change `href` from `"/services/websites"` → `"/contact"`

### Add a new Monthly Plans card below the Website Creation card:
After the Website Creation `<article>`, add a new article card (same styling pattern as the other cards, use `md:col-span-2` like the website card) for the monthly add-on plans:

```tsx
<article className="rounded-2xl border border-white/10 bg-[#12121A] p-8 md:col-span-2 h-full flex flex-col">
  <p className="text-sm uppercase tracking-wide text-[#8B5CF6] mb-3">Monthly Plans</p>
  <h2 className="text-3xl font-semibold mb-2">Add-on to any website</h2>
  <p className="text-[#A1A1AA] mb-6">Keep your site running and growing after launch.</p>
  <div className="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
    <div className="rounded-xl p-5 border border-white/10 bg-[#0F0F16] h-full flex flex-col">
      <p className="text-sm uppercase tracking-wide text-[#8B5CF6] mb-1">Hosting</p>
      <p className="text-2xl font-semibold mb-3">$50<span className="text-base text-[#A1A1AA]">/mo</span></p>
      <ul className="space-y-2 text-sm text-[#A1A1AA] flex-1">
        <li className="flex items-start gap-2.5"><Check className="h-4 w-4 shrink-0 mt-0.5 text-[#8B5CF6]" aria-hidden /><span>Hosting + uptime monitoring</span></li>
        <li className="flex items-start gap-2.5"><Check className="h-4 w-4 shrink-0 mt-0.5 text-[#8B5CF6]" aria-hidden /><span>1 site edit per month</span></li>
      </ul>
    </div>
    <div className="rounded-xl p-5 border border-[#8B5CF6]/50 bg-[#0A0A0F] h-full flex flex-col">
      <p className="text-sm uppercase tracking-wide text-[#8B5CF6] mb-1">Growth</p>
      <p className="text-2xl font-semibold mb-3">$299<span className="text-base text-[#A1A1AA]">/mo</span></p>
      <ul className="space-y-2 text-sm text-[#A1A1AA] flex-1">
        <li className="flex items-start gap-2.5"><Check className="h-4 w-4 shrink-0 mt-0.5 text-[#8B5CF6]" aria-hidden /><span>Everything in Hosting</span></li>
        <li className="flex items-start gap-2.5"><Check className="h-4 w-4 shrink-0 mt-0.5 text-[#8B5CF6]" aria-hidden /><span>2 blog posts per month</span></li>
        <li className="flex items-start gap-2.5"><Check className="h-4 w-4 shrink-0 mt-0.5 text-[#8B5CF6]" aria-hidden /><span>Google Business profile updates</span></li>
        <li className="flex items-start gap-2.5"><Check className="h-4 w-4 shrink-0 mt-0.5 text-[#8B5CF6]" aria-hidden /><span>Local SEO optimization</span></li>
        <li className="flex items-start gap-2.5"><Check className="h-4 w-4 shrink-0 mt-0.5 text-[#8B5CF6]" aria-hidden /><span>Monthly performance report</span></li>
      </ul>
    </div>
  </div>
  <Link href="/contact" className="inline-flex w-fit mt-auto px-6 py-3 rounded-full bg-gradient-to-r from-[#8B5CF6] to-[#A78BFA] text-white font-semibold">
    Get a Free Quote
  </Link>
</article>
```

### Update the Review Funnel card:
- Change h2 from `"Starter $79/mo"` → `"$79/mo"`
- Remove the line `"Growth $149/mo • Pro is a contact-us plan."`
- Change the description to: `"Automatic review follow-ups after every job. Set it and forget it."`
- Change CTA from `"Get Started"` → `"Get More Reviews"` and keep href to `/portal/checkout?product=review_funnel`
- Remove the secondary "Learn More" button entirely

### Remove the SEO & Content card:
Delete the entire `<article>` for "SEO & Content" — this is now covered by the Monthly Plans card above.

### Update the Custom Apps card:
- Change CTA from `"Book a Consultation"` → `"Let's Talk"` (keep href `/contact`)

---

## 2. `src/components/PricingOverview.tsx`

### Replace the `tiers` array:

```ts
const tiers = [
  {
    title: "Start Free",
    product: "Cadence Phone Answering",
    price: "$199/mo after 7-day free trial",
    cta: { label: "Call Cadence Live", href: "tel:+14806313993" },
  },
  {
    title: "Website Plans",
    product: "Launch $500 • Enterprise $1,000",
    price: "One-time build + optional monthly plans",
    cta: { label: "Get a Free Quote", href: "/contact" },
  },
  {
    title: "Grow Every Month",
    product: "Hosting $50/mo • Growth $299/mo",
    price: "Blog posts, local SEO, monthly reports",
    cta: { label: "Get a Free Quote", href: "/contact" },
  },
];
```

### Remove the Website Plans bullet list:
Delete the conditional block `{tier.title === "Website Plans" && (<>...</>)}` that renders bullets for "Launch: quick launch essentials" etc. and the migration upcharge note. The tier cards should all render uniformly.

---

## Gate
Run `npm run build` from the project root. It must pass with zero errors.

## After Gate
Commit with message: `feat: update pricing page + pricing overview to match contract`
Push to `ui/autom8-redesign`.

Then notify Steve by writing this JSON to a temp file and POSTing:
```json
{"message":"Batch complete. Gate ready.","agentId":"main","deliver":false}
```
```
curl.exe --data-binary @<tempfile> -H "Content-Type: application/json" -H "Authorization: Bearer KyCiiWTXATTqCayTrPyoE9krcBzXFP7YPtzqEGlA1aA=" http://localhost:18789/hooks/agent
```
