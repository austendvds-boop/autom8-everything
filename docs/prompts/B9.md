# B9 — CTA + Footer + ShimmerButton + StickyMobileCTA + ScrollFade

**Thinking level:** medium
**Branch:** `ui/full-redesign`
**Gate:** `Set-Location 'C:\Users\austen\.openclaw\workspace-coder\projects\autom8-everything'; npm run build`

---

## Overview

Color overhaul on the final page sections: CTA section (full amber gradient background), Footer, ShimmerButton, StickyMobileCTA, and ScrollFade. After this batch, every component on the homepage uses the warm amber token system — zero purple/cyan remaining.

---

## Files to Modify

1. `src/components/CTA.tsx`
2. `src/components/Footer.tsx`
3. `src/components/ShimmerButton.tsx`
4. `src/components/StickyMobileCTA.tsx`
5. `src/components/ScrollFade.tsx`
6. `src/components/TiltCard.tsx` (color check only)

---

## 1. `src/components/CTA.tsx` — Amber Gradient Background

### Background
Replace the purple/cyan gradient with warm amber:
```tsx
<div className="absolute inset-0 bg-gradient-to-br from-[#D4A030] via-[#B8892A] to-[#0E1015]">
```

### Gradient drift animation
Replace the animated gradient colors:
```tsx
animate={{
  background: [
    "radial-gradient(circle at 30% 30%, rgba(212, 160, 48, 0.5) 0%, transparent 50%)",
    "radial-gradient(circle at 70% 60%, rgba(232, 192, 104, 0.5) 0%, transparent 50%)",
    "radial-gradient(circle at 40% 80%, rgba(184, 137, 42, 0.5) 0%, transparent 50%)",
    "radial-gradient(circle at 30% 30%, rgba(212, 160, 48, 0.5) 0%, transparent 50%)",
  ],
}}
transition={{ duration: 20, repeat: Infinity, ease: "easeInOut" }}
```

### Typography
- H2: `fontFamily: "var(--font-manrope), sans-serif"` (replace Playfair)
- H2 color: `text-white` (keep white on gradient bg)
- Subhead: `text-white/80`
- Trust signals: `text-white/70`

### Buttons
The ShimmerButton component is used here. Its internal colors will be fixed in section 3 below. The CTA section just needs to pass correct props.

---

## 2. `src/components/Footer.tsx` — Color Overhaul

### Background
- `bg-[#0A0A0F]` → `bg-[#0E1015]`
- Border top: `border-white/5` (keep)

### Colors
| Old | New |
|-----|-----|
| `#8B5CF6` | `#D4A030` |
| `#A1A1AA` | `#9B978F` |
| `#52525B` | `#5E5B56` |
| `#1A1A23` | `#1E2028` |
| `#71717A` | `#5E5B56` |

### Social icons
- Default: `text-[#9B978F]`
- Hover: `hover:text-[#D4A030] hover:border-[#D4A030]/50`
- Background: `bg-[#1E2028]`

### Phone link hover
- `hover:text-[#D4A030]` instead of `hover:text-[#8B5CF6]`

### Font
- Any `fontFamily: "var(--font-playfair)"` → `fontFamily: "var(--font-manrope), sans-serif"`

### BrandLogo
BrandLogo was already updated in B5 — just make sure Footer uses it correctly. No changes needed to the BrandLogo import/usage.

---

## 3. `src/components/ShimmerButton.tsx` — Amber Shimmer

Read the file first. Apply these changes:

- Any purple gradient: `from-[#8B5CF6] to-[#A78BFA]` → `from-[#D4A030] to-[#E8C068]`
- Primary variant: amber gradient background, dark text (`text-[#0E1015]`)
- Secondary variant: outline with white text, amber border on hover
- Shimmer effect: keep the white shimmer sweep (it works on any bg)
- Hover glow: `rgba(212, 160, 48, 0.3)` instead of purple

---

## 4. `src/components/StickyMobileCTA.tsx` — Amber Colors

Read the file. Apply:
- Button: amber gradient, dark text
- Background bar: glass bg with warm tones
- Any purple → amber
- Any `#0A0A0F` → `#0E1015`

---

## 5. `src/components/ScrollFade.tsx` — Background Update

Read the file. Change:
- Any background references from old colors to `#0E1015`
- If it uses gradient fades from section to section, update gradient stops to use `#0E1015`

---

## 6. `src/components/TiltCard.tsx` — Color Check

Read the file. If it has any hardcoded purple colors or references, replace with amber. If it's color-agnostic (just handles tilt transform), no changes needed.

---

## Final Sweep

After modifying all files, do a project-wide search for any remaining instances of these OLD values in `src/components/` and `src/app/` files:
- `#8B5CF6`
- `#A78BFA`
- `#06B6D4`
- `#0A0A0F`
- `#111118`
- `#12121A`
- `font-playfair`

If any are found in homepage-related files, fix them. Product detail pages (`/services/*`), portal pages, blog pages, and API routes can keep old colors for now — they'll inherit globals.css changes automatically for CSS classes, and inline colors on sub-pages are out of scope for this redesign.

---

## Gate

Run: `Set-Location 'C:\Users\austen\.openclaw\workspace-coder\projects\autom8-everything'; npm run build`

Build must pass.

---

## Git

Commit message: `feat: CTA amber gradient, footer warm colors, shimmer/sticky/scroll amber overhaul`

Push to `ui/full-redesign`.

---

## Procurement Contract

Every coder batch must terminate immediately after the required gates for that batch are satisfied and the code is committed and pushed. Coders must not start local servers, run manual verification loops, open-ended exploratory tests, or invent post-gate validation steps.

Every batch must end with a git commit and push to the project branch. If a batch makes no code changes, the coder must write a status/log entry (e.g., append to docs/deploy-log.md) and commit+push it.

Coders MUST notify Steve as the final step after git push by POSTing to `http://localhost:18789/hooks/agent` with `Authorization: Bearer KyCiiWTXATTqCayTrPyoE9krcBzXFP7YPtzqEGlA1aA=` and body `{"message":"Batch complete. Gate ready.","agentId":"main","deliver":false}`. Write the JSON to a temp file and use `curl.exe --data-binary @file` to avoid shell escaping issues.

Coders do NOT need to set up git credentials. The orchestrator handles PAT configuration pre-flight.
