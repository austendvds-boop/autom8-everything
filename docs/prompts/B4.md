# B4: HowItWorks Rewrite + Plain Language Audit

**Thinking level: low**
**Gate:** `npm run build`
**Branch:** `ui/autom8-redesign`

## Critical Constraint
**Do NOT change any visual styling** — no color changes, no font changes, no layout changes, no animation changes, no spacing changes. Only change text content. The 3-column grid, numbered circles, icon boxes, connector lines, and motion presets must remain exactly as-is.

## Files to Edit
1. `src/components/HowItWorks.tsx` (primary)
2. Audit pass: `src/components/Hero.tsx`, `src/components/ServicesBento.tsx`, `src/components/OfferLadder.tsx`, `src/components/Footer.tsx`

## Changes

### HowItWorks.tsx — Replace the `steps` array:

```ts
const steps: Step[] = [
  {
    number: "01",
    title: "Tell Us About Your Business",
    description:
      "Quick call, 15 minutes. We figure out which tools make sense and what will get you results fastest.",
    icon: ClipboardList,
  },
  {
    number: "02",
    title: "We Set Everything Up",
    description:
      "Live in days, not months. We handle all the technical work — you don't touch a thing.",
    icon: Wrench,
  },
  {
    number: "03",
    title: "Customers Start Reaching You",
    description:
      "Calls answered, reviews coming in, rankings climbing. We keep optimizing every month.",
    icon: PhoneCall,
  },
];
```

### HowItWorks.tsx — Update section subheading:
Keep the current subheading: `"Simple steps. Clear timeline. No technical work on your side."` — it already matches contract tone.

### HowItWorks.tsx — Update CTA button:
Change the CTA text from `"See Pricing"` → `"See Our Products"` and change `href` from `"#offer-ladder"` → `"#services"`.

### Plain Language Audit Pass

Scan ALL of these files for any remaining banned phrases and fix them:

**Banned phrases to search for and remove/replace:**
- "Growth Stack" → remove or replace with specific product names
- "infrastructure" → remove
- "lead machine" → "a website that brings in customers"
- "growth audit" → "free quote" or "15-minute call"
- "ecosystem" → remove
- "seamless" → remove
- "robust" → remove
- "solution" / "solutions" → say what it actually does
- "leverage" → remove
- "scale your business" → "grow your business" if needed
- "all-in-one platform" → remove
- "Learn More" CTA → "How It Works" or specific action
- "Get Started" CTA (on product cards) → use contract-approved CTA for that product
- "Discover" → remove

**Specific known issues from current code:**
1. Hero.tsx: eyebrow should say "Tools for local businesses" (verify B1 changed it)
2. ServicesBento.tsx: verify no old product copy references "Growth Stack"
3. OfferLadder.tsx: verify heading says "Simple Pricing. No Surprises."
4. Footer.tsx: verify no "Growth Audit" language remains

If all banned phrases are already gone from B1-B3 changes, and no other issues found, just confirm in a commit message that the audit passed clean.

## Gate
Run `npm run build` from the project root. It must pass with zero errors.

## After Gate
Commit with message: `feat: rewrite how-it-works + plain language audit`
Push to `ui/autom8-redesign`.

Then notify Steve by writing this JSON to a temp file and POSTing:
```json
{"message":"Batch complete. Gate ready.","agentId":"main","deliver":false}
```
```
curl.exe --data-binary @<tempfile> -H "Content-Type: application/json" -H "Authorization: Bearer KyCiiWTXATTqCayTrPyoE9krcBzXFP7YPtzqEGlA1aA=" http://localhost:18789/hooks/agent
```
