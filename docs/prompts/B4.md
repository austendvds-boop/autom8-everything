# B4 — Foundation: CSS Token System + Typography + Font Loading

**Thinking level:** high
**Branch:** `ui/full-redesign`
**Gate:** `Set-Location 'C:\Users\austen\.openclaw\workspace-coder\projects\autom8-everything'; npm run build`

---

## Overview

Replace the entire purple/cyan/cold color system with the warm amber/charcoal palette. Update fonts (add Manrope, remove Playfair Display). Update design-tokens.ts and motion.ts. This is the foundation batch — every subsequent batch depends on these tokens being correct.

**The build WILL have TypeScript/lint warnings after this batch because components still reference old colors inline. That's fine — the build must pass, not be warning-free. Later batches fix component colors.**

---

## Files to Modify

1. `src/app/globals.css` — full rewrite
2. `src/app/layout.tsx` — font loading update
3. `src/lib/design-tokens.ts` — full rewrite
4. `src/lib/motion.ts` — update values to match blueprint

---

## 1. `src/app/globals.css` — Full Rewrite

Replace the entire file. Keep the `@import "tailwindcss"` at top. Here is the complete new content:

```css
@import "tailwindcss";

/* ========================================
   Autom8 Everything — Design Tokens
   Warm dark + amber accent system
   ======================================== */

:root {
  /* Background Tones */
  --bg-primary: #0E1015;
  --bg-surface: #161920;
  --bg-elevated: #1E2028;
  --bg-glass: rgba(22, 25, 32, 0.80);

  /* Accent (Amber) */
  --accent: #D4A030;
  --accent-hover: #E5B544;
  --accent-muted: #B8892A;
  --accent-subtle: rgba(212, 160, 48, 0.10);
  --accent-glow: rgba(212, 160, 48, 0.20);

  /* Text */
  --text-primary: #EDEBE8;
  --text-secondary: #9B978F;
  --text-muted: #5E5B56;

  /* Borders */
  --border-default: rgba(255, 255, 255, 0.06);
  --border-hover: rgba(212, 160, 48, 0.30);
  --border-active: rgba(212, 160, 48, 0.50);

  /* Semantic */
  --success: #4ADE80;
  --error: #F87171;
  --warning: #FBBF24;
  --star: #F59E0B;

  /* Gradients */
  --gradient-warm: linear-gradient(135deg, #D4A030 0%, #E8C068 100%);
  --gradient-glow: radial-gradient(ellipse at center, rgba(212, 160, 48, 0.15) 0%, transparent 70%);

  /* Glassmorphism */
  --glass-bg: rgba(22, 25, 32, 0.80);
  --glass-border: 1px solid rgba(255, 255, 255, 0.08);
  --glass-blur: blur(12px);

  /* Shadows */
  --shadow-glow: 0 0 60px var(--accent-glow);
  --shadow-card: 0 4px 24px rgba(0, 0, 0, 0.4);
  --shadow-card-hover: 0 0 40px var(--accent-glow);

  /* Border Radius */
  --radius-sm: 6px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 24px;
  --radius-full: 9999px;

  /* Animation */
  --ease-default: cubic-bezier(0.4, 0, 0.2, 1);
  --ease-in: cubic-bezier(0.4, 0, 1, 1);
  --ease-out: cubic-bezier(0, 0, 0.2, 1);

  /* Motion tokens */
  --motion-reveal: 0.8s;
  --motion-ease: cubic-bezier(0.16, 1, 0.3, 1);
  --motion-hover: 0.3s;
  --motion-hover-ease: cubic-bezier(0.4, 0, 0.2, 1);
  --motion-stagger-text: 0.06s;
  --motion-stagger-card: 0.1s;

  /* Section Spacing */
  --section-py-sm: 5rem;
  --section-py-md: 7rem;
  --section-py-lg: 8rem;
  --section-gap: 1.5rem;
}

@theme inline {
  --color-background: var(--bg-primary);
  --color-foreground: var(--text-primary);
  --color-bg-primary: var(--bg-primary);
  --color-bg-surface: var(--bg-surface);
  --color-accent: var(--accent);
}

* {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  background: var(--bg-primary);
  color: var(--text-primary);
  font-family: var(--font-inter), -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  overflow-x: hidden;
}

:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}

/* Custom Scrollbar */
::-webkit-scrollbar {
  width: 8px;
}
::-webkit-scrollbar-track {
  background: var(--bg-primary);
}
::-webkit-scrollbar-thumb {
  background: var(--bg-elevated);
  border-radius: var(--radius-full);
}
::-webkit-scrollbar-thumb:hover {
  background: var(--accent);
}

/* Glass Utility */
.glass {
  background: var(--glass-bg);
  backdrop-filter: var(--glass-blur);
  -webkit-backdrop-filter: var(--glass-blur);
  border: var(--glass-border);
}

.glass-card {
  position: relative;
  overflow: hidden;
}

.glass-card::before {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: inherit;
  padding: 1px;
  background: linear-gradient(135deg, rgba(212, 160, 48, 0.3), transparent 40%, transparent 60%, rgba(212, 160, 48, 0.15));
  -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
  -webkit-mask-composite: xor;
  mask-composite: exclude;
  opacity: 0;
  transition: opacity 0.3s;
}

.glass-card:hover::before {
  opacity: 1;
}

/* Gradient Text — amber gradient on key words */
.gradient-text {
  background: var(--gradient-warm);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* Glow Effects */
.glow-amber {
  box-shadow: var(--shadow-glow);
}

/* Mesh Background — hero only */
.mesh-bg {
  background:
    radial-gradient(at 30% 20%, rgba(212, 160, 48, 0.18) 0px, transparent 50%),
    radial-gradient(at 70% 10%, rgba(232, 192, 104, 0.10) 0px, transparent 50%),
    radial-gradient(at 10% 60%, rgba(184, 137, 42, 0.12) 0px, transparent 50%),
    radial-gradient(at 80% 50%, rgba(212, 160, 48, 0.08) 0px, transparent 50%),
    radial-gradient(at 50% 90%, rgba(232, 192, 104, 0.06) 0px, transparent 50%),
    var(--bg-primary);
}

.section-glow {
  position: absolute;
  width: 600px;
  height: 600px;
  border-radius: 50%;
  filter: blur(120px);
  opacity: 0.10;
  pointer-events: none;
  will-change: transform;
}

.section-glow--amber { background: #D4A030; }
.section-glow--warm { background: linear-gradient(135deg, #D4A030, #E8C068); }

/* Grid Pattern */
.grid-pattern {
  background-image:
    linear-gradient(rgba(255, 255, 255, 0.03) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255, 255, 255, 0.03) 1px, transparent 1px);
  background-size: 50px 50px;
}

/* Animation Keyframes */
@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-20px); }
}

@keyframes pulse-glow {
  0%, 100% { opacity: 0.7; }
  50% { opacity: 1; }
}

@keyframes shimmer {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}

.shimmer-btn {
  position: relative;
  overflow: hidden;
}

.shimmer-btn::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.15), transparent);
  transition: none;
}

.shimmer-btn:hover::before {
  animation: shimmer-sweep 0.6s ease-out forwards;
}

@keyframes shimmer-sweep {
  to { left: 100%; }
}

.shimmer-btn::after {
  content: '';
  position: absolute;
  inset: 0;
  background: radial-gradient(circle at 50% 50%, rgba(255,255,255,0.2) 0%, transparent 60%);
  opacity: 0;
  transform: scale(0);
  border-radius: inherit;
}

.shimmer-btn:active::after {
  animation: ripple 0.4s ease-out forwards;
}

@keyframes ripple {
  to { opacity: 0; transform: scale(2.5); }
}

@keyframes star-twinkle {
  0%, 100% { opacity: 0.85; }
  50% { opacity: 1; }
}

.star-twinkle {
  animation: star-twinkle 3.4s ease-in-out infinite;
  will-change: opacity;
}

/* Reduced Motion */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}

/* Card System */
.card-base {
  border-radius: var(--radius-lg);
  border: 1px solid var(--border-default);
  background: var(--bg-surface);
}

.card-elevated {
  border-radius: 1.5rem;
  border: 1px solid var(--border-default);
  background: var(--bg-surface);
  transition: all 0.3s ease;
}

.card-elevated:hover {
  border-color: var(--border-hover);
  box-shadow: 0 0 0 1px var(--border-hover), var(--shadow-card-hover);
}

/* Button System */
.btn-primary {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 3rem;
  padding: 0.625rem 2rem;
  border-radius: 9999px;
  background: var(--gradient-warm);
  color: var(--bg-primary);
  font-weight: 600;
  font-size: 0.9375rem;
  transition: all 0.2s ease;
}

.btn-primary:hover {
  box-shadow: 0 0 30px var(--accent-glow);
}

.btn-secondary {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 3rem;
  padding: 0.625rem 2rem;
  border-radius: 9999px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  color: var(--text-primary);
  font-weight: 600;
  font-size: 0.9375rem;
  transition: all 0.2s ease;
}

.btn-secondary:hover {
  border-color: var(--border-active);
}

.btn-ghost {
  display: inline-flex;
  align-items: center;
  color: var(--text-secondary);
  font-weight: 500;
  transition: color 0.2s ease;
}

.btn-ghost:hover {
  color: var(--text-primary);
}

/* Typography */
.section-heading {
  font-size: 2rem;
  line-height: 1.1;
  font-weight: 700;
  letter-spacing: -0.02em;
  font-family: var(--font-manrope), sans-serif;
  color: var(--text-primary);
}

@media (min-width: 768px) {
  .section-heading {
    font-size: 3.5rem;
  }
}

@media (min-width: 1024px) {
  .section-heading {
    font-size: 3.5rem;
  }
}

.section-subheading {
  color: var(--text-secondary);
  font-size: 1.125rem;
  max-width: 42rem;
}

@media (min-width: 768px) {
  .section-subheading {
    font-size: 1.25rem;
  }
}
```

**Key changes from old globals.css:**
- All purple (#8B5CF6) → amber (#D4A030)
- All cyan (#06B6D4) → removed entirely (single accent system)
- Background #0A0A0F → #0E1015 (warm near-black)
- Surface #12121A → #161920 (warm)
- Text #FFFFFF → #EDEBE8 (warm off-white)
- Text secondary #A1A1AA → #9B978F (warm gray)
- Text muted #52525B → #5E5B56 (warm muted)
- Card bg #111118 → var(--bg-surface)
- `.gradient-text` now uses amber gradient
- `.glass-card` hover border now amber
- `.mesh-bg` now uses amber/warm radials
- `.section-glow--purple` → `.section-glow--amber`
- `.btn-primary` now amber gradient with dark text
- `.section-heading` now uses `var(--font-manrope)` instead of `var(--font-playfair)`
- Focus outline now amber

---

## 2. `src/app/layout.tsx` — Font Loading

Replace the font imports and variables. Changes:

1. **Remove** the `Playfair_Display` import and `playfair` constant entirely
2. **Add** Manrope import:

```typescript
import { Inter, JetBrains_Mono, Manrope } from "next/font/google";
```

3. **Add** Manrope font constant (replace the playfair one):

```typescript
const manrope = Manrope({
  variable: "--font-manrope",
  subsets: ["latin"],
  display: "swap",
  weight: ["600", "700", "800"],
});
```

4. **Update** the `<body>` className to use manrope instead of playfair:

```tsx
className={`${inter.variable} ${jetbrainsMono.variable} ${manrope.variable} antialiased`}
```

5. **Remove** any `<link>` tags for Playfair Display if present in `<head>`.

Do NOT change the schema JSON, metadata, or any other layout logic.

---

## 3. `src/lib/design-tokens.ts` — Full Rewrite

Replace the entire file:

```typescript
/** Shared design tokens — warm amber/charcoal system */

export const SECTION_PY = {
  sm: 'py-20',
  md: 'py-28',
  lg: 'py-32',
} as const;

export const CARD_BASE = 'rounded-2xl border border-white/[0.06] bg-[#161920]';

export const CARD_ELEVATED =
  'rounded-3xl border border-white/[0.06] bg-[#161920] hover:border-[#D4A030]/30 hover:shadow-[0_0_40px_rgba(212,160,48,0.20)] transition-all duration-300';

export const COLORS = {
  bgPrimary: '#0E1015',
  bgSurface: '#161920',
  bgElevated: '#1E2028',
  accent: '#D4A030',
  accentHover: '#E5B544',
  accentMuted: '#B8892A',
  accentSubtle: 'rgba(212, 160, 48, 0.10)',
  accentGlow: 'rgba(212, 160, 48, 0.20)',
  textPrimary: '#EDEBE8',
  textSecondary: '#9B978F',
  textMuted: '#5E5B56',
  borderDefault: 'rgba(255, 255, 255, 0.06)',
  borderHover: 'rgba(212, 160, 48, 0.30)',
  borderActive: 'rgba(212, 160, 48, 0.50)',
  star: '#F59E0B',
} as const;
```

---

## 4. `src/lib/motion.ts` — Update Values

Update the file to match the blueprint motion spec. Replace the entire file:

```typescript
import type { Variants } from "framer-motion";

const easeExpoOut = [0.16, 1, 0.3, 1] as const;
const easeStandard = [0.4, 0, 0.2, 1] as const;

// Spring configs
export const springSmooth = { type: "spring" as const, stiffness: 100, damping: 20, mass: 0.5 };
export const springSnappy = { type: "spring" as const, stiffness: 400, damping: 25 };
export const springDramatic = { type: "spring" as const, stiffness: 80, damping: 15 };

// Viewport config
export const viewportOnce = { once: true, margin: "-80px" as const };

// Variant objects
export const fadeUp: Variants = {
  hidden: { opacity: 0, y: 30 },
  visible: { opacity: 1, y: 0, transition: { duration: 0.8, ease: easeExpoOut } },
};

export const fadeIn: Variants = {
  hidden: { opacity: 0 },
  visible: { opacity: 1, transition: { duration: 0.6, ease: easeExpoOut } },
};

export const scaleIn: Variants = {
  hidden: { opacity: 0, scale: 0.97 },
  visible: { opacity: 1, scale: 1, transition: springSmooth },
};

export const slideInLeft: Variants = {
  hidden: { opacity: 0, x: -40 },
  visible: { opacity: 1, x: 0, transition: { duration: 0.8, ease: easeExpoOut } },
};

export const slideInRight: Variants = {
  hidden: { opacity: 0, x: 40 },
  visible: { opacity: 1, x: 0, transition: { duration: 0.8, ease: easeExpoOut } },
};

export const staggerContainer: Variants = {
  hidden: {},
  visible: {
    transition: {
      staggerChildren: 0.1,
      delayChildren: 0.05,
    },
  },
};

export const staggerItem: Variants = {
  hidden: { opacity: 0, y: 30, scale: 0.97 },
  visible: { opacity: 1, y: 0, scale: 1, transition: springSmooth },
};

export const reveal = {
  initial: { opacity: 0, y: 40 },
  whileInView: { opacity: 1, y: 0 },
  viewport: viewportOnce,
  transition: { duration: 0.8, ease: easeExpoOut },
};

export const revealReduced = {
  initial: { opacity: 0 },
  whileInView: { opacity: 1 },
  viewport: viewportOnce,
  transition: { duration: 0.4, ease: easeExpoOut },
};

export const revealStagger = (index: number, reduced: boolean | null | undefined = false) => {
  const base = reduced ? revealReduced : reveal;
  return {
    ...base,
    transition: { ...base.transition, delay: index * 0.1 },
  };
};

export const cardHover = {
  whileHover: { y: -4, transition: { duration: 0.3, ease: easeStandard } },
};

export const buttonHover = {
  whileHover: { scale: 1.03 },
  whileTap: { scale: 0.97 },
  transition: springSnappy,
};

export const textLineReveal = (lineIndex: number): Variants => ({
  hidden: { opacity: 0, y: 20 },
  visible: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.6, ease: easeExpoOut, delay: lineIndex * 0.06 },
  },
});
```

---

## Gate

Run: `Set-Location 'C:\Users\austen\.openclaw\workspace-coder\projects\autom8-everything'; npm run build`

The build must pass. Components will still have inline purple hex values — that's expected and will be fixed in B5–B9.

---

## Git

Commit all changes with message: `feat: warm amber/charcoal token system, Manrope typography, motion updates`

Push to `ui/full-redesign`.

---

## Procurement Contract

Every coder batch must terminate immediately after the required gates for that batch are satisfied and the code is committed and pushed. Coders must not start local servers, run manual verification loops, open-ended exploratory tests, or invent post-gate validation steps.

Every batch must end with a git commit and push to the project branch. If a batch makes no code changes, the coder must write a status/log entry (e.g., append to docs/deploy-log.md) and commit+push it.

Coders MUST notify Steve as the final step after git push by POSTing to `http://localhost:18789/hooks/agent` with `Authorization: Bearer KyCiiWTXATTqCayTrPyoE9krcBzXFP7YPtzqEGlA1aA=` and body `{"message":"Batch complete. Gate ready.","agentId":"main","deliver":false}`. Write the JSON to a temp file and use `curl.exe --data-binary @file` to avoid shell escaping issues.

Coders do NOT need to set up git credentials. The orchestrator handles PAT configuration pre-flight.
