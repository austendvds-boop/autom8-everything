# B6: Websites Service Page + SEO Content Page Rework

**Thinking level: medium**
**Gate:** `npm run build`
**Branch:** `ui/autom8-redesign`

## Critical Constraint
**Do NOT change any visual styling** — no color changes, no font changes, no layout changes, no animation changes, no spacing changes. Only change text content, data arrays, links, and CTAs. Keep all existing Tailwind classes, component structure, and motion presets exactly as-is.

## Files to Edit
1. `src/app/services/websites/page.tsx`
2. `src/app/services/seo-content/page.tsx`

---

## 1. `src/app/services/websites/page.tsx`

### Replace the `tiers` array:

The old array has Launch $1,500 / Scale $3,500 (recommended) / Custom "Let's Talk". Replace with the contract structure — 2 one-time website tiers + 2 monthly plans:

```ts
const websiteTiers = [
  {
    name: "Launch",
    bestFor: "New businesses that need a professional site fast",
    description: "A clean, mobile-ready website that makes your business look credible and easy to contact.",
    price: "$500",
    priceNote: "one-time",
    highlights: [
      "Up to 5 pages",
      "Mobile-ready design",
      "Contact or booking form",
      "Basic on-page SEO",
    ],
  },
  {
    name: "Enterprise",
    bestFor: "Businesses that need more pages and custom features",
    description: "Everything in Launch plus custom features, more pages, and room to grow.",
    price: "$1,000",
    priceNote: "one-time",
    highlights: [
      "Up to 10 pages",
      "Everything in Launch",
      "Custom features and functionality",
      "Built for your specific workflow",
    ],
    recommended: true,
  },
];

const monthlyPlans = [
  {
    name: "Hosting",
    price: "$50",
    priceNote: "/mo",
    description: "Keep your site running smoothly after launch.",
    highlights: [
      "Hosting + uptime monitoring",
      "1 site edit per month",
    ],
  },
  {
    name: "Growth",
    price: "$299",
    priceNote: "/mo",
    description: "Hosting plus blog content, local SEO, and monthly reports so customers find you on Google.",
    highlights: [
      "Everything in Hosting",
      "2 blog posts published per month",
      "Google Business profile updates",
      "Local SEO optimization",
      "Monthly performance report",
    ],
    recommended: true,
  },
];
```

### Update the pricing section to render both grids:

The `#pricing` section currently renders a 3-column grid of `tiers`. Change it to:

1. A "Website Build" heading with a 2-column grid for `websiteTiers` (change `md:grid-cols-3` → `md:grid-cols-2`)
2. Below that, a "Monthly Plans" sub-heading with a 2-column grid for `monthlyPlans`
3. Keep the same card styling pattern (`card-base`, bullet list with `•`, etc.)
4. For the monthly plans cards, show `$50/mo` and `$299/mo` format in the price display

### Update hero copy:
- h1: Keep as-is — `"A Website That Gets Picked, Trusted, and Contacted."` — good
- Subheadline: Change to `"We build professional websites for local businesses. Your site will be mobile-ready, easy to find on Google, and designed to get you more calls."`
- Change hero CTA from `"Get Started"` → `"Get a Free Quote"`
- Change secondary CTA from `"View Tiers"` → `"See Pricing"`

### Update the mid-page CTA:
- Change `"Ready to upgrade? Let's pick your tier."` → `"Know what you need? Let's get started."`
- Change CTA from `"Get Started"` → `"Get a Free Quote"`

### Update metadata:
- Change description to: `"Professional business websites starting at $500. Mobile-ready, SEO-friendly, built for local businesses."`

### Update service schema description:
- Change to: `"Professional website builds for local businesses with Launch ($500) and Enterprise ($1,000) tiers plus monthly hosting and growth plans."`

### Update the final CTA section:
- Keep heading as-is
- Change CTAs: first button `"Call (480) 631-3993"` is fine, change second from `"Fill Out the Form"` to `"Get a Free Quote"` (keep href `/contact`)

### Update FAQ:
Replace the `faqs` array:
```ts
const faqs = [
  {
    question: "Can you rebuild my existing site?",
    answer: "Yes. We can migrate and rebuild older websites. Ask us for a quote.",
  },
  {
    question: "Do I need to write all the content?",
    answer: "No. We handle the writing and structure. You just tell us about your business.",
  },
  {
    question: "How long does a website take to build?",
    answer: "Most Launch sites are live in about a week. Enterprise projects take 2-3 weeks.",
  },
  {
    question: "Can I start with Launch and add Growth later?",
    answer: "Yes. Many businesses start with a Launch site and Hosting, then add Growth when they are ready to invest in SEO.",
  },
  {
    question: "What is the difference between Hosting and Growth?",
    answer: "Hosting keeps your site running and includes 1 edit per month. Growth adds blog posts, Google Business updates, local SEO, and a monthly report so you show up in more searches.",
  },
];
```

---

## 2. `src/app/services/seo-content/page.tsx`

This page currently positions SEO as a standalone "Monthly Retainer, Contact Us" service. Per the contract, SEO/content is the Growth $299/mo add-on to a website. Rewrite this page to serve as the detailed breakdown of the Growth plan.

### Update hero:
- Eyebrow: Keep `"SEO & Content"`
- h1: `"Show Up When Customers Search for What You Do."`
- Subheadline: `"We publish fresh content to your site every month and keep your Google Business profile up to date. Customers find you instead of your competition."`
- CTA: Change `"Contact Us"` → `"Get a Free Quote"` (href stays `/contact`)
- Secondary CTA: keep `"How It Works"`

### Update the pricing section:
Replace the "Monthly Retainer / Contact Us" card with the actual Growth plan pricing:

```tsx
<p className="text-sm uppercase tracking-wide text-[#8B5CF6] mb-2">Growth Plan</p>
<p className="text-4xl font-bold mb-3">$299<span className="text-xl text-[#A1A1AA]">/month</span></p>
<p className="text-[#A1A1AA] mb-6">Add-on to any website plan. Includes hosting.</p>
```

Add a bullet list of what's included:
- Hosting + uptime monitoring
- 1 site edit per month
- 2 blog posts published per month
- Google Business profile updates
- Local SEO optimization
- Monthly performance report

CTA: `"Get a Free Quote"` → `/contact`

Remove the "What affects pricing" list since it's now a fixed-price plan.

### Update deliverables section:
The `deliverables` array currently has 5 items. Update to match contract:
```ts
const deliverables = [
  { title: "2 blog posts per month", description: "Targeting local search terms your customers already use.", icon: FileText },
  { title: "Google Business profile updates", description: "Keep your listing current so you show up in the map pack.", icon: MapPin },
  { title: "Local SEO optimization", description: "On-page improvements so your pages rank for local searches.", icon: Search },
  { title: "Monthly performance report", description: "Clear numbers on rankings, traffic, and what's working.", icon: BarChart3 },
  { title: "Hosting + 1 site edit per month", description: "Your site stays up, fast, and current.", icon: Globe },
];
```

### Fix banned language throughout the page:
- Remove `"Build Your Full Growth Stack"` CTA text → `"Get a Free Quote"`
- Remove the "SEO Works Best as Part of Your Growth Stack" section heading → `"SEO Works Best Alongside Your Other Tools"` 
- Remove word "compounding" from any copy — use "builds over time" or "grows month over month"
- Remove "local signals" — use "local SEO"
- Change any `"Contact Us"` CTA labels to `"Get a Free Quote"` (except the final one which can stay as "Get a Free Quote")

### Update metadata:
- Change description to: `"Monthly local SEO and blog content for $299/mo. Show up in local Google searches and get more calls."`

---

## Gate
Run `npm run build` from the project root. It must pass with zero errors.

## After Gate
Commit with message: `feat: rewrite websites + seo-content pages to match contract pricing`
Push to `ui/autom8-redesign`.

Then notify Steve by writing this JSON to a temp file and POSTing:
```json
{"message":"Batch complete. Gate ready.","agentId":"main","deliver":false}
```
```
curl.exe --data-binary @<tempfile> -H "Content-Type: application/json" -H "Authorization: Bearer KyCiiWTXATTqCayTrPyoE9krcBzXFP7YPtzqEGlA1aA=" http://localhost:18789/hooks/agent
```
