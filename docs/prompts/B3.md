# B3: OfferLadder (Pricing Cards) Rewrite

**Thinking level: medium**
**Gate:** `npm run build`
**Branch:** `ui/autom8-redesign`

## Critical Constraint
**Do NOT change any visual styling** — no color changes, no font changes, no layout changes, no animation changes, no spacing changes. Only change the `tiers` data array, section heading text, and section subheading text. The card rendering logic, styles, grid layout, featured badge logic, and motion presets must remain exactly as-is.

## File to Edit
`src/components/OfferLadder.tsx`

## Changes

### Replace the `tiers` array:

```ts
const tiers: Tier[] = [
  {
    name: "AI Receptionist",
    product: "Cadence",
    price: "$199/mo",
    valueProp: "Never miss a customer call",
    features: [
      "Answers calls around the clock",
      "Books appointments automatically",
      "Sounds like a real person",
      "7-day free trial, no contracts",
    ],
    cta: { label: "Call Cadence Live", href: "tel:+14806313993" },
    icon: PhoneCall,
  },
  {
    name: "Reviews",
    product: "Review Funnel",
    price: "$79/mo",
    valueProp: "Turn happy customers into 5-star reviews",
    features: [
      "Automated follow-up after each job",
      "Happy customers sent to Google",
      "More reviews = better rankings",
      "Works while you sleep",
    ],
    cta: { label: "Get More Reviews", href: "/services/review-funnel" },
    icon: MessageSquareHeart,
  },
  {
    name: "Websites + SEO",
    product: "Web + Monthly SEO",
    price: "From $500",
    valueProp: "Get found on Google",
    features: [
      "Launch website: $500 (up to 5 pages)",
      "Enterprise website: $1,000 (up to 10 pages)",
      "Hosting: $50/mo",
      "Growth plan: $299/mo (blog + SEO + reports)",
    ],
    cta: { label: "Get a Free Quote", href: "/contact" },
    icon: Globe,
    featured: true,
    badge: "Most Popular",
  },
  {
    name: "Built for You",
    product: "Custom Apps",
    price: "Custom Quote",
    valueProp: "If it saves time or makes money, we build it",
    features: [
      "Scrapers, dashboards, automations",
      "Internal tools for your workflow",
      "Scoped after a short call",
      "No templates — built from scratch",
    ],
    cta: { label: "Let's Talk", href: "/contact" },
    icon: Wrench,
  },
];
```

### Update section heading and subheading:
- Change `<h2>` text from `"One Platform. Pick Your Starting Point."` → `"Simple Pricing. No Surprises."`
- Change `<p>` subheading from `"Start with what you need today. Add more when you're ready."` → `"Every price is right here. No hidden fees, no long contracts."`

### Note on CTA labels:
The contract says "Get a Free Quote" only for Web + Monthly SEO, and "Let's Talk" for Custom Apps. Do NOT use "Learn More", "Get Started", or "Discover" as CTA labels.

## Gate
Run `npm run build` from the project root. It must pass with zero errors.

## After Gate
Commit with message: `feat: rewrite pricing cards per site contract`
Push to `ui/autom8-redesign`.

Then notify Steve by writing this JSON to a temp file and POSTing:
```json
{"message":"Batch complete. Gate ready.","agentId":"main","deliver":false}
```
```
curl.exe --data-binary @<tempfile> -H "Content-Type: application/json" -H "Authorization: Bearer KyCiiWTXATTqCayTrPyoE9krcBzXFP7YPtzqEGlA1aA=" http://localhost:18789/hooks/agent
```
